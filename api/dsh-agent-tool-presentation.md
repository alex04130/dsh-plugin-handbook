# dsh-agent-tool-presentation

> Agent-plane presentation selector: composes one agent's tools as PTC mode, native, or both
> 已知限制：只读 .d.ts（纯 JS 导出不可见）；不含参数语义（只有签名与行号）。
> 生成：2026-09-11（源 mtime · content-7536a635） · DSH 0.1.5。

## dsh-agent-tool-presentation/lib/types/index.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| const | name | `export declare const name = "tool-presentation"` | 24 |
| const | inject | `export declare const inject: string[]` | 30 |
| interface | Config | `export interface Config` | 32 |
| const | Config | `export declare const Config: z<Config>` | 43 |
| function | apply | `export declare function apply(ctx: Context, config: Config): void` | 49 |

