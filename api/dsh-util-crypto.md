# dsh-util-crypto

> Zero-dependency browser-safe UUID and byte-encoding helpers
> 已知限制：只读 .d.ts（纯 JS 导出不可见）；不含参数语义（只有签名与行号）。
> 生成：2026-09-11（源 mtime · content-7536a635） · DSH 0.1.5。

## dsh-util-crypto/lib/types/index.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| type | Uuid | `export type Uuid` | 12 |
| function | bytesToBase64 | `export declare function bytesToBase64(data: Uint8Array): string` | 18 |
| function | randomUUID | `export declare function randomUUID(): Uuid` | 23 |

