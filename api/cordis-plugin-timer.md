# cordis-plugin-timer

> Timer service for cordis
> 已知限制：只读 .d.ts（纯 JS 导出不可见）；不含参数语义（只有签名与行号）。
> 生成：2026-09-11（源 mtime · content-7536a635） · DSH 0.1.5。

## cordis-plugin-timer/lib/types/index.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| type | WithDispose | `type WithDispose<T>` | 7 |
| class | TimerService | `export declare class TimerService extends Service` | 11 |
| method | TimerService.constructor | `constructor(ctx: Context);` | 12 |
| default | TimerService | `export default TimerService;` | 29 |

