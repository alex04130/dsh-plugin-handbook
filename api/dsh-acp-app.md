# dsh-acp-app

> The dsh ACP profile bundle: automation-only JSON-RPC stdio and process lifecycle over dsh-base
> 已知限制：只读 .d.ts（纯 JS 导出不可见）；不含参数语义（只有签名与行号）。
> 生成：2026-09-11（源 mtime · content-7536a635） · DSH 0.1.5。

## dsh-acp-app/lib/types/index.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| const | name | `export declare const name = "acp-app-startup"` | 9 |
| const | inject | `export declare const inject: string[]` | 11 |
| const | ACP_APP_STARTUP_SERVICE | `export declare const ACP_APP_STARTUP_SERVICE = "acpAppStartup"` | 13 |
| function | apply | `export declare function apply(ctx: Context): void` | 19 |

