# dsh-session-checkpoint-policy

> Semantic session durability checkpoints before model requests and tool side effects
> 已知限制：只读 .d.ts（纯 JS 导出不可见）；不含参数语义（只有签名与行号）。
> 生成：2026-09-11（源 mtime · content-7536a635） · DSH 0.1.5。

## dsh-session-checkpoint-policy/lib/types/index.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| const | name | `export declare const name = "session-checkpoint-policy"` | 8 |
| const | inject | `export declare const inject: string[]` | 10 |
| function | apply | `export declare function apply(ctx: Context): void` | 22 |

