# dsh-tool-present

> Explicit workspace file delivery declarations for the DeepSeek Harness
> 已知限制：只读 .d.ts（纯 JS 导出不可见）；不含参数语义（只有签名与行号）。
> 生成：2026-09-11（源 mtime · content-7536a635） · DSH 0.1.5。

## dsh-tool-present/lib/types/index.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| const | name | `export declare const name = "tool-present"` | 5 |
| interface | Config | `export interface Config` | 7 |
| const | Config | `export declare const Config: z<Config>` | 12 |
| const | inject | `export declare const inject: string[]` | 14 |
| function | apply | `export declare function apply(ctx: Context, config: Config): void` | 20 |

## dsh-tool-present/lib/types/types.d.ts

| 类型 | 名称 | 签名 | 行 |
|---|---|---|---|
| interface | PresentedFile | `export interface PresentedFile` | 4 |

